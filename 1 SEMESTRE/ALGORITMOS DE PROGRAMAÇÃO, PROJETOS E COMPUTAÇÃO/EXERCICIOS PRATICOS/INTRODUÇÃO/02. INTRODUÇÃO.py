'''
Determine a área de um triângulo
'''

base = float(input('Digite o valor da base do triangulo: '))
altura = float(input('Digite o valor da altura do triangulo: '))

area = (base*altura)/2

print(f'a area do triangulo de base: {base} e altura: {altura} é igual a {area}')
